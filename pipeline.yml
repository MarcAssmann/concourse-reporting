---
resources:
- name: scripts
  type: git
  source:
    uri: https://github.com/MarcAssmann/concourse-reporting.git

jobs:
- name: reporting-job
  public: true
  plan:
  - get: scripts
  - task: reporting-task
    platform: linux
    image_resource:
      type: docker-image
      source: {repository: bobohund/buildtools}
    inputs:
      - name: scripts
    run:
      path: echo
      args: [HELLO]
